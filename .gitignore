# Credenciales reales de Supabase (usa supabaseConfig.example.js como plantilla)
js/core/supabaseConfig.js

# Prototipo de la Fase 1 anterior, conservado solo como referencia local
legacy/

# Sistema operativo / editor
.DS_Store
Thumbs.db

# Configuración local del entorno de desarrollo (no forma parte del producto)
.claude/
